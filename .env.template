# eBay Developer Credentials Template
# 
# To get these credentials:
# 1. Go to https://developer.ebay.com/my/keys
# 2. Create a new application (if you don't have one)
# 3. Copy the credentials from your application dashboard
# 4. Create a .env file in the root directory with these values

# eBay Application Credentials
# Your eBay App ID (Client ID) - required for all API access
EBAY_APP_ID=your-app-id-here

# Your eBay Developer ID - required for some legacy APIs
EBAY_DEV_ID=your-dev-id-here

# Your eBay Certificate ID (Client Secret) - KEEP THIS SECURE!
# This is equivalent to a password and should never be shared
EBAY_CERT_ID=your-cert-id-here

# Environment Configuration
# Set to 'true' for sandbox testing, 'false' for production
EBAY_SANDBOX_MODE=true

# eBay marketplace site (usually EBAY-US)
EBAY_SITE_ID=EBAY-US

# Transport Configuration
# Options: stdio (default), sse, http
LOOTLY_TRANSPORT=stdio

# Host and port for network transports (only used for sse/http)
LOOTLY_HOST=127.0.0.1
LOOTLY_PORT=8000

# Optional: Redis URL for distributed caching
# REDIS_URL=redis://localhost:6379/0

# Optional: Override default API settings
# EBAY_API_VERSION=1.13.0
# EBAY_TIMEOUT=30
# EBAY_MAX_RETRIES=3
# EBAY_CACHE_TTL=300
# EBAY_RATE_LIMIT_PER_DAY=5000
# EBAY_PAGE_SIZE=50
# EBAY_MAX_PAGES=10